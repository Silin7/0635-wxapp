<van-popup show="{{overlayShow}}" position="bottom">
  <van-picker
    show-toolbar
    title="{{pickerTitle}}"
    columns="{{dataList}}"
    bind:cancel="onCancel"
    bind:confirm="onConfirm"
    item-height="60"/>
</van-popup>