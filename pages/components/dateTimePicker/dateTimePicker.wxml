<van-popup show="{{overlayShow}}" position="bottom">
  <van-datetime-picker
    type="{{dateType}}"
    value="{{currentDate}}"
    bind:cancel="cancelDate"
    bind:confirm="confirmDate"
    item-height="60"/>
</van-popup>