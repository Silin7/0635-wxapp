<van-popup show="{{overlayShow}}" position="bottom">
  <van-datetime-picker
    type="{{dateType}}"
    value="{{currentDate}}"
    min-date="{{minDate}}"
    max-date="{{maxDate}}"
    bind:cancel="cancelDate"
    bind:confirm="confirmDate"
    item-height="60"/>
</van-popup>