<van-toast id="van-toast" />
<van-dialog id="van-dialog" />

<!-- 图片列表篇 -->
<van-image
  wx:for="{{imageList}}"
  wx:key="index"
  width="100vw"
  height="300px"
  src="{{item.imageUrl}}"
  bind:click="bPicture"
  data-item="{{item}}"
/>